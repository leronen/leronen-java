package util.collections.graph;

// import java.util.List;

/** 
 * A node of a undirected graph. Implementations may assume 
 * that nodes are "unique", that is equality comparison with "==" is consistent
 * with equals and hashCode
 * 
 * Hmm, is this ever needed, or would it be better to use parametrized types
 * always? Let all operations be defined in IDirectedGraph?
 */
public interface INode {
    // foo 
    // get rid of this, would this imply?
}
